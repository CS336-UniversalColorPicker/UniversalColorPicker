import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cs336.universalcolorpicker',
  appName: 'UniversalColorPicker',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
